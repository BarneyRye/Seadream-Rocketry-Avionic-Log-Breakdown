# Seadream-Rocketry-Avionic-Log-Breakdown

Used in conjucntion with "https://github.com/BarneyRye/Seadream-Rocketry-RTOS.git" to convert the output logs into usable and understandable files<br/>
The .c files converts the output log files intto various outputs.<br/>
the .m file then takes the EKF#.csv and plots it on a 3D axis to view the trajectory
